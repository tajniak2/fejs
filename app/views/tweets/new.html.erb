<h1>Dodaj wpis</h1>

<div class="row">
  <div>
    <%= form_for(@tweet) do |f| %>
	  <%= render 'shared/error_messages', object: f.object %>

      <%= f.label :email, "Status" %>
      <%= f.text_field :status %>
      <br />
	  <%= f.submit "Dodaj" %>
    <% end %>
  </div>
</div>